#include <stdio.h>
#include <stdlib.h>
{void double p
    p=-(x*x)+(3*x);
    double (double v);
    v=(2*x*x*x)-(x*x)-(5*x); }
    double a,b,n;
    scanf("%lf%lf%lf",&a&b&n);
    sum=0;
int s[n];
scanf ("%lf",&s[n]);
for(i=0;i<n;i++); 
scanf("%lf",s[i]);
float t=b-a/n;
sum=sum+(abs(p-v))*t;
printf("%lf",sum);
	return 0;
}